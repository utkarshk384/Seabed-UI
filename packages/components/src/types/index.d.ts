declare module "@seabedui/components" {
	import type { CSSType } from "@seabedui/types"
	const styles = {
		base: CSSType,
		components: CSSType,
		reset: CSSType,
		utilities: CSSType,
	}

	export default styles
}
